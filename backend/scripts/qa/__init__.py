"""color_qa_harness.plan.md: the Phase-1 base-quality QA harness. Read-only
diagnostic tooling that lives entirely under `backend/scripts/` -- `qa/` is
the one reusable, DB-free metrics module the `_diag_qa_*.py` scripts share.
"""
