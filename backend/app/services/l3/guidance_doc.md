<!--
  GUIDANCE DOC (format-specific editing styles) -- Edso's reference.

  Scope: this file holds ONLY format-specific editing STYLES -- "if it's a
  podcast, edit like this; if it's a reel, like this; here are the special
  cases." It is a LOOKUP REFERENCE the brain consults while planning, not a
  rulebook. General craft (what good looks like, the move vocabulary, pacing
  philosophy, continuity/junk meaning) lives in the system prompt, not here.

  How it's used: this whole file is injected into Edso's system prompt (cached)
  and referenced in the planning step of the workflow. The brain self-selects
  the relevant section from the project summary + the user's ask, and BLENDS or
  OVERRIDES when the material or the user says otherwise. Keep each entry to a
  few sentences framed as "lean toward", never a rigid template.

  STATUS: scaffold only -- sections are placeholders to be written. Until a
  section has real content, the brain falls back to General / mixed.
-->

# Format guidance

## Reel / short-form
_TODO: style intent for vertical short-form. (placeholder)_

## Talking-head / interview
_TODO: style intent for single-subject talking-head. (placeholder)_

## Podcast / multicam
Conversation filmed from several fixed angles. The alternate angles of one
speaker are OUTLOOKS (same words, different camera), not takes — never treat
them as retries and never crown a "winner" among them; pick the best angle per
beat.

Lean toward: keep the CURRENT speaker on screen, choosing the angle with the
highest total_quality (this already biases to the on-camera close-up). Hold an
angle for the whole thought rather than cutting on every pause — let delivery,
not the clock, drive the change. On a speaker change, cut to the new speaker's
best angle at their first word.

Rapid back-and-forth (fast turn-taking, overlaps, reactions): lean to the
widest shot that holds both people so the exchange reads without whip-cutting.
There's no fixed trigger for "rapid" — judge it from the turn pattern on the
go; if it's a close call and the choice matters, ask the user rather than
guess. When no true wide angle exists (all cameras are single-person), stay on
whoever is currently speaking and cut on each turn. Cutaways/B-roll only to
cover a real disfluency gap, not for variety.

## Montage / B-roll
_TODO: style intent for music-led montage / B-roll. (placeholder)_

## Special cases
_TODO: notable exceptions and how to handle them. (placeholder)_

## General / mixed (fallback)
_TODO: default lean when the footage is ambiguous or a mix of the above.
(placeholder)_
