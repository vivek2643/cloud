fastapi==0.115.12
uvicorn[standard]==0.34.0
python-dotenv==1.1.0
pydantic-settings==2.8.1
supabase==2.13.0
boto3==1.37.14
python-multipart==0.0.20
PyJWT==2.10.1
httpx==0.28.1

# --- Phase 1: L1 indexing ---
procrastinate[psycopg]==3.7.0
faster-whisper==1.1.1
scenedetect[opencv]==0.6.5
transformers==4.49.0
# SigLIP's tokenizer (SiglipTokenizer) is SentencePiece-backed; transformers
# does NOT pull these in automatically, so the embeddings stage crashes without
# them. protobuf is required by the SentencePiece -> fast-tokenizer conversion.
sentencepiece==0.2.0
protobuf==5.29.3
# CPU build of torch; on Apple Silicon this resolves to the universal wheel.
torch==2.6.0
torchvision==0.21.0
librosa==0.10.2.post1
opencv-python-headless==4.11.0.86
pydub==0.25.1
pgvector==0.3.6
# Direct psycopg for raw queries we need outside the Supabase REST client
psycopg[binary]==3.2.4
numpy<2.0

# --- Phase 3a: L3 query parsing ---
anthropic==0.49.0

# --- Phase 2: L2 stages ---
# Stage B (faces): SCRFD + ArcFace via insightface
insightface==0.7.3
onnxruntime==1.20.1
# Stage C (audio events): YAMNet via the PyTorch port
panns-inference==0.1.1
# Stage D (narratives): self-hosted Qwen2.5-VL on the worker GPU.
# transformers 4.49 ships Qwen2_5_VLForConditionalGeneration; qwen-vl-utils
# handles image pre-processing, accelerate enables low_cpu_mem_usage loading.
qwen-vl-utils==0.0.10
accelerate==1.4.0
