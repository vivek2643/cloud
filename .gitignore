# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/
env/

# Environment
.env
.env.local
.env.*.local

# Build
.next/
out/
dist/
build/
*.egg-info/
*.tsbuildinfo

# Runtime logs (analysis/edit/render output written at runtime)
backend/logs/

# color_qa_harness.plan.md: generated corpus stills/scoreboards/contact
# sheets -- local artifacts by design (A.5/Part C: "commit the JSON to
# _out/qa/baselines/<date>.json locally, not to git prod code").
backend/scripts/_out/

# cpd_boundary_segmenter.plan.md: downloaded GEBD videos/annotations,
# extracted per-clip feature matrices, and the trained model artifact --
# large binary/data files, regenerated locally by the cpd/*.py scripts,
# never committed. Keep the eval report (a small text/json summary) out of
# this list if it's ever written under data/ -- move it elsewhere to commit.
backend/scripts/cpd/data/
backend/models/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Cursor internal (skills, worktrees)
.claude/

# Modal
.modal/

# Misc
*.log
coverage/
.vercel

# Source assets (pitch deck, design sources — app uses frontend/public/)
Assets/

# Separate trail — not part of this app
website/

# Remotion demo video — local only, not part of the app
demo/
