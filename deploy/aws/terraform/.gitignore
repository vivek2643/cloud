# Terraform local state, plugin cache, and secrets
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Real var files may contain secrets; keep only the example
*.tfvars
*.tfvars.json
!*.tfvars.example

override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
